import { ErrorInputDirective } from './error-input.directive';

describe('ErrorInputDirective', () => {
  it('should create an instance', () => {
    const directive = new ErrorInputDirective();
    expect(directive).toBeTruthy();
  });
});
